#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen_recursion - stdout lenght of string
 * @s: string value
 *
 * Return: int
 */
int _strlen_recursion(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
