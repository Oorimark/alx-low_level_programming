#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strlen - prints the lenght of a string
 * @s : argument *s
 *
 * Return: int
 */
int _strlen(char *s)
{
	int res = (int) strlen(s);

	return (res);
}
