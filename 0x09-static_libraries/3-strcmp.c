#include <string.h>
#include "main.h"

/**
 * _strcmp - compares two strings
 * @s1: first argument
 * @s2: second argument
 *
 * Return: int
 */
int _strcmp(char *s1, char *s2)
{
	int res = strcmp(s1, s2);

	return (res);
}
